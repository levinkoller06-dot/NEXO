# NEXO project entry

Code and its working rules are now in `Code/`. Read `Code/AGENTS.md` and `Code/STATUS.md` before changes.
Obsidian planning remains in `Planung/NEXO-Obsidian-Plan/` at repository root.
For every completed implementation or planning step, create a new numbered note in its `Schritte/` folder, update current status, commit and push. Verify remote HEAD. Do not start unrelated work without user instruction.
